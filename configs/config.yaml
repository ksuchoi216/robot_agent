llm:
  model_name: gpt-5-mini
  temperature: null
  max_tokens: 2048

retry:
  max_retries: 2

paths:
  output_dir: "output/"
  prompt_dir: "src/prompts/"

parsing:
  goal_regex: "^(\\d+)\\.\\s*(.*)$"
  task_regex: "^(\\d+)\\.\\s*(.*)$"
  action_regex: "^(\\d+)\\.\\s*(.*)$"
